# Production build-time env (Vite auto-loads this for `vite build`).
# VITE_* values are inlined into the client bundle, so this is NOT secret.
# Points the Sage chat widget at the deployed FastAPI backend (via cloudflared).
VITE_SAGE_API_URL=https://api.scalesage.ai
