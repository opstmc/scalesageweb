# See https://help.dev/cli for more information.
.DS_Store
dist
dist-ssr
.output
node_modules
*.local
.dev.vars
.wrangler

# local env (keep secrets out of git)
.env
.env.local
.env.*.local
# .env.production holds only the PUBLIC build-time VITE_SAGE_API_URL (baked into
# client JS anyway), so it is committed for deterministic CI builds.
!.env.production
!.env.example
